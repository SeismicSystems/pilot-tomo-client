services:
    client:
        build:
            context: .
            target: client-arbitrum-sepolia
        environment:
            ENDPOINT: http://44.203.177.203:3001
        networks:
            - default
        healthcheck:
            test: ["CMD", "cat", "/usr/src/app/contract/out/deploy.json"]
            interval: 5s
            timeout: 5s
            retries: 3
networks:
    server:
        name: tomo-server-network
        external: true
